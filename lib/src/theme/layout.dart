import 'package:flutter/material.dart';

const defaultBorderRadius = BorderRadius.all(Radius.circular(16));
const EdgeInsets defaultEdgeInsets = EdgeInsets.all(defaultSpace);
const Widget defaultSeparator =
    SizedBox(width: defaultSpace, height: defaultSpace);
const double defaultSpace = 16.0;
const largeBorderRadius = BorderRadius.all(Radius.circular(24));

const EdgeInsets largeEdgeInsets = EdgeInsets.all(largeSpace);
const largeIcon = 48.0;
const Widget largeSeperator = SizedBox(width: largeSpace, height: largeSpace);
const double largeSpace = 24.0;

const smallBorderRadius = BorderRadius.all(Radius.circular(smallSpace));
const EdgeInsets smallEdgeInsets = EdgeInsets.all(smallSpace);
const smallRadius = Radius.circular(smallSpace);
const Widget smallSeperator = SizedBox(width: smallSpace, height: smallSpace);
const double smallSpace = 8.0;
